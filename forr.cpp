#include <iostream>
#include <stdlib.h>
using namespace std;
int main()
{
    system ("cls");
    int x;

    cout << "Inputkan sebuah angka : ";
    cin >> x;

    // inisialisasi, kondisi, iterasi ( a=3 )
    for (int a = x; a <= 10; a++)
    {
        cout << a << endl;
    }

    // proses 1 : a -> 1 lalu di cek kondisi yang benar
    // lalu tampil baru ditambah
}